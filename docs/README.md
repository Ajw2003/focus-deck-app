# Docs

Focus Deck's documentation lives in six numbered tiers under `docs/`, plus `plans/` (live work)
and `archive/` (finished, inert work).

Start here: [`1-landing/README.md`](1-landing/README.md) — the full index of what exists and
where.
